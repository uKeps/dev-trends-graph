import GraphView from "@/components/GraphView";

/**
 * Página principal — renderiza o grafo de tendências em tela cheia.
 * O GraphView é um Client Component que utiliza React Flow.
 */
export default function HomePage() {
  return <GraphView />;
}
