import GraphView from "@/components/GraphView";

/**
 * Main page — renders the trends graph full screen.
 * GraphView is a Client Component built on React Flow.
 */
export default function HomePage() {
  return <GraphView />;
}
